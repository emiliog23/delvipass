services:
  - type: web
    name: delvipass-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install --include=dev && npm run build
    startCommand: npx prisma migrate resolve --rolled-back 20260620000000_add_password_reset; npm start
    disk:
      name: delvipass-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        value: file:/data/prod.db
      - key: UPLOAD_DIR
        value: /data/uploads/events
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        sync: false
